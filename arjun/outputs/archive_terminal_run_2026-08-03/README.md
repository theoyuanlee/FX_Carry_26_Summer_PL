# Archived terminal run — 2026-08-03

One notebook, kept as the record of a superseded run.

`hedge_comparison_with_JY1SF1SPXT.ipynb` is the version of the hedge comparison that ran against a
Bloomberg pull including the `JY1`, `SF1` and `SPXT` futures tickers, before the analysis was
re-cut. It is superseded by [`../../hedge_comparison.ipynb`](../../hedge_comparison.ipynb) in the
parent folder, which is the version the verdict rests on.

It is archived rather than deleted because its outputs are the ones that were on screen when the
futures-hedge question was first discussed, and the conclusion did not change: no investable futures
overlay — static or dynamic — beats simple de-risking of the book on the same stress trigger.
