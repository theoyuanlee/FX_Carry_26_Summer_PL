# requirements.txt — cesare/ folder of the FX Carry project
#
# Scope: ONLY the packages that cesare/ actually imports (grepped from fx_utils.py
# and the 7 stage notebooks) plus the Jupyter runtime needed to execute/build them.
# Loose >= pins; the trailing comment on each line is the version this was developed
# against. Grouped and tagged SHARED / CESARE-UNIQUE for clean dedup when the repo's
# per-person requirements are later collated into ONE root requirements.txt.
# Python 3.13 (dev env: 3.13.5).

# --- Core scientific / data stack (SHARED across the repo) ---
numpy>=2.1          # 2.1.3
pandas>=2.2         # 2.2.3
scipy>=1.15         # 1.15.3   (scipy.optimize.minimize — mvo_weights, Stage 4)
statsmodels>=0.14   # 0.14.6   (Newey-West HAC regressions)

# --- Parquet engine (SHARED) ---
# pyarrow is used INDIRECTLY via pandas' pd.read_parquet (never imported by name).
# CAVEAT: it must be pip-installed OVER conda in this env — conda's pyarrow 19.x
# cannot read these files; the working build is pyarrow 24.x. See project plan
# §5.2 and the project memory note.
pyarrow>=24.0       # 24.0.0

# --- Plotting ---
matplotlib>=3.10    # 3.10.0   (SHARED)
seaborn>=0.13       # 0.13.2   (CESARE-UNIQUE — only data_visualization.ipynb imports it)

# --- Notebook runtime (needed to run/build the .ipynb; not imported in code) ---
jupyter>=1.1        # 1.1.1
ipykernel>=6.29     # 6.29.5
nbformat>=5.10      # 5.10.4
